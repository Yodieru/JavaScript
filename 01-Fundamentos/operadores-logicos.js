/* 

  Operadores logicos
  
  Estos operadores perm,iten evaluar expresiones que dan como resultado verdadero o falso
  
  Una expresion es un trozo de codigo que evalua, es decir resulta "true" o "false".

  1. AND (&&) - Devueleve 'true' solo si ambos operadores son verdaderos
  2. OR (||) - Devuelve 'true' si almenos uno de los operandos es verdadero
  3. NOT (!) - Invierte el valor booleano. Se escribe del lado izquierdo de la variable sin espacios.

*/

let a = 5 < 1;
let b = 5 < 7 ;

// console.log( (a) && (b)); // true

//(console.log( (a) || (b) ) // false

console.log ( !b ) // falses